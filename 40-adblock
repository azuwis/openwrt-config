opkg_install uhttpd

if [ "$(opkg info adblock | wc -l)" -gt 0 ]; then
    opkg_install adblock
else
    opkg_install /tmp/adblock.ipk
fi

if [ ! -e /var/dnsmasq.d/adb_list.adaway ]; then
    service start adblock
fi

add_cron '0 4 * * 0,3 /etc/init.d/adblock start'
