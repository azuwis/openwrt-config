opkg_install uhttpd

opkg_installed adblock || opkg_update
if [ "$(opkg info adblock | wc -l)" -gt 0 ]; then
    opkg_install adblock
else
    opkg_install /tmp/adblock.ipk
fi

uci set adblock.global.adb_forcedns='0'
service restart adblock

add_cron adblock '0 4 * * 0,3 /etc/init.d/adblock start'
